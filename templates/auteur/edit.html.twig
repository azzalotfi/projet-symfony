{% extends 'base.html.twig' %}

{% block title %}Modifier l'auteur{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="card">
        <div class="card-header">
            <div class="d-flex justify-content-between align-items-center">
                <h1 class="h3 mb-0">Modifier l'auteur</h1>
                <div>
                    <a href="{{ path('app_auteur_index') }}" class="btn btn-secondary me-2">Retour à la liste</a>
                    <a href="{{ path('app_auteur_show', {'id': auteur.id}) }}" class="btn btn-info">Voir les détails</a>
                </div>
            </div>
        </div>
        <div class="card-body">
            {{ include('auteur/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
        <div class="card-footer">
            {{ include('auteur/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
